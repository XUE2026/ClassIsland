# 操作记录

## 2026-06-27

### 构建 Android APK 历程

1. **分支**: `android_environment` - 所有 ClassIsland Android 移植代码
2. **工具链**: Kotlin 1.9.22 + AGP 8.2.2 + Compose BOM 2024.01.00
3. **构建平台**: GitHub Actions (setup-android@v4, JDK 17)

### 构建问题修复记录

| 尝试 | Run ID | 结果 | 问题 | 修复 |
|------|--------|------|------|------|
| #1-15 | 多次 | ❌ | setup-android v3 不兼容 | 升级到 v4 |
| #16 | 28288542237 | ✅ 构建成功 | APK 未上传(artifact 路径错误) | 调整 upload path |
| #17+ | 多次 | ❌/✅ | 多个问题 | 见下 |
| #28290937843 | 构建成功 | ✅ 构建通过 | 但无 APK 产物上传 | 需要排查路径 |
| #28292187328 | 构建成功 | ✅ | resource name `import` 冲突 | 改为 `import_data` |
| #28292636593 | **构建成功** | ✅ | **Kotlin 编译错误已全部修复** | 见详细修复列表 |

### Kotlin 编译错误修复
1. `GpuAccelerator.kt`: `and0xFF` → `and 0xFF` (缺少空格)
2. `HorizontalDivider` → `Divider` (Compose BOM 2024.01.00 无 HorizontalDivider)
3. `MainActivity.kt`: 移动 `showLicense` 到 `setContent` 块内
4. `MainActivity.kt`: NavigationBarItem 使用命名参数
5. `OverlayView.kt`: 添加 ViewGroup/WindowManager 导入
6. `AboutScreen.kt`: Icons.Default.GitHub → Icons.Default.Code

### 当前状态
- 编译通过 ✅
- APK 生成于 `app/build/outputs/apk/debug/`
- 使用 GitHub Actions artifact 上传
- 需手动下载 APK