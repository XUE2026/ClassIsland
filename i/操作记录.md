# 操作记录

## 2026-06-28

### Android APK 构建 Action 发布

- **任务**: 在 `android*` 分支上发布 GitHub Action 来构建 APK
- **操作详情**:
  1. 检查了现有工作目录结构，发现已有 `.github/workflows/build-android-apk.yml` 文件
  2. 检查了 `ClassIsland.Android` 项目结构，包括 Gradle 配置、Rust 原生库构建脚本、AndroidManifest.xml 等
  3. 更新了 `build-android-apk.yml` Action 配置：
     - 触发条件扩展到所有 `android**` 分支（push 和 pull_request）
     - 保留 `workflow_dispatch` 手动触发，支持选择 debug/release 构建类型
     - 新增 `build-rust` Job：自动安装 Rust 工具链、Android NDK，编译 4 个 ABI 架构的 Rust 原生库（arm64-v8a, armeabi-v7a, x86_64, x86）
     - 新增 Rust 和 Gradle 缓存加速构建
     - debug 构建输出未签名 APK
     - release 构建支持通过 secrets 签名（ANDROID_KEYSTORE_BASE64 等）
     - 构建产物上传为 GitHub Artifact
     - release + tag 时自动发布到 GitHub Release